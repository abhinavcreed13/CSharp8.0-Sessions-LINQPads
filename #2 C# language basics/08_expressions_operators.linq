<Query Kind="Expression" />

// -- Expression - essentially denotes a value

// 12 * 30 <- expression

// Primary expressions - Math.Log(1)

// Void expressions - Console.WriteLine(1)

// Assignment expressions -> x = x * 5

// -- Operators
// precedence !!
// +, *, - .....

// 1 + 2 * 3 ? -> Precedence?

// if 1 + 2 < 3 + 4 ? -> if 3 < 7 -> answer

// if a == b < c == d? -> if a(13) == true/false == d ? // runtime-error

// if a == b && c == d -> if true/false && true/false